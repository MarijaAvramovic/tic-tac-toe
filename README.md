# tic-tac-toe
Stored   the gameboard as an array inside of a Gameboard object,  players are stored in objects.
Used factories. IIFE (module pattern) not used.
   included logic that checks for when the game is over, checking   3-in-a-rows and ties.  included taking user input.
included functions that allow players to add marks to a specific spot on the board by interacting with the appropriate DOM elements (e.g. letting players click on a board square to place their marker). used the logic that keeps players from playing in spots that are already taken!
Clean up the interface to allow players to put in their names, include a button to start/restart the game and add a display element that shows the results upon game end!

pseudo code

table 9 cells
2 players  x and o
1 turn per player 
on click fill empty cell 
colect cells check value and if 3 in row add point for winner 

time spent 5x2h